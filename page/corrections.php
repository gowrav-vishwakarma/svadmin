<?php

class page_corrections extends Page {
	function init(){
	parent::init();
/*
		ALTER TABLE `xdistributors` ADD `Total_Amount` INT NOT NULL DEFAULT '0',
		ADD `Service_Charge` FLOAT NOT NULL DEFAULT '0',
		ADD `TDS` FLOAT NOT NULL DEFAULT '0',
		ADD `Net_Amount` FLOAT NOT NULL DEFAULT '0'
*/	
	}
}